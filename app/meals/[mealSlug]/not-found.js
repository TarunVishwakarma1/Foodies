'use client';

export default function NotFound({error}) {
    return(
        <main className="not-found">
            <h1>Meal Not Found</h1>
            <p>The Meal you are trying to make, cannot be found now. Please try again.</p>
        </main>
    );
}