'use client';

export default function NotFound(){
    return(
        <main className="not-found">
            <h1>
                Page Cannot be found
            </h1>
            <p>
                Unfortunately, the requested page or resource cannot be found!
            </p>
        </main>
    );
}